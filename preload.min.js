const{contextBridge:contextBridge,remote:remote}=require("electron");const{dialog:dialog}=require("electron").remote;const win=remote.getCurrentWindow();contextBridge.exposeInMainWorld("winCtl",{close:()=>{win.close()},max:()=>{if(win.isFullScreen())win.setFullScreen(false);else win.setFullScreen(true)},min:()=>{win.minimize()},restore:()=>{win.unmaximize()}});contextBridge.exposeInMainWorld("fileOps",{filePicker:(customTitle,dialogOps)=>{return dialog.showOpenDialog(win,{title:customTitle,message:customTitle,properties:dialogOps})}});require("electron");